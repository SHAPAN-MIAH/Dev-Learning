import React from "react";
import Nav from "./Nav";
import classes from ;

const Layout = () => {
  return (
    <>
      <Nav />
      <main className={classes.main}>
      </main>
    </>
  );
};

export default Layout;
